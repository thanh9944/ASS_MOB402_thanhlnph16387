<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <% include css %>
  <title>Hello, world!</title>
</head>
<body style="background-color: #d9d5d5" >
<div style="background-color: white">
  <h3 style="color: blue;padding-top: 100px;padding-bottom: 40px;box-shadow: gray ;
   padding-left: 120px">Hình nền điện thoại đẹp cho dế yêu của bạn.</h3>
</div>

<div class="container">
<%- include nav %>
  <div class="row">

    <% for (var i = 0; i < assData.length; i++) { %>
      <div class="col">

        <div style="width: 400px;height:300px ;background-image: url( '<%= assData[i].linkPhoto %>') ;background-size:cover;">
          <form style="margin-left: 330px" method="post" action="/delete-car"
                enctype="application/x-www-form-urlencoded">
            <input name="idCar" hidden value="<%= assData[i].id %>">
            <button style="width: 70px" type="submit">Delete</button>
          </form>


          <form style="margin-left: 330px" method="post" action="/update-car"
                enctype="application/x-www-form-urlencoded">
            <input name="idCar" hidden value="<%= assData[i].id %>">
            <input hidden name="name" value="<%= assData[i].name %>" >
            <input hidden name="content" value="<%= assData[i].content %>" >
            <input hidden name="linkPhoto" value="<%= assData[i].linkPhoto %>">
            <input hidden name="date" value="<%= assData[i].date %>">
            <button style="width: 70px" type="submit">Update</button>
          </form>

        </div>
        <p style="margin-bottom: 30px "><%= assData[i].name %></p>
      </div>
    <% } %>

  </div>


</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>

</body>
</html>